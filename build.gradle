plugins {
    id 'java'
    id 'org.sonarqube' version '25.12.0.117093'
}

sonar {
    properties {
        property "sonar.projectKey", "portfolio"
        property "sonar.host.url", "http://localhost:9000"
    }
}
